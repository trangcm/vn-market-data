"""Benchmarks for the package, and the timing harness the whole suite shares.

Not shipped in the wheel (see `packages` in ``pyproject.toml``) — like ``tests/``,
these live inside the package so they travel with the subtree split, not so they
land on a user's disk.
"""
